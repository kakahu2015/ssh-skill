# .secrets/<主机名>.env
# 复制此文件为 .secrets/<你的主机名>.env 并填写真实连接信息。
# 示例文件可提交；真实 .env 已由 .gitignore 排除。

# Required for all hosts. Can be real IP or domain.
HOST=1.2.3.4

# Required for key auth.
KEY_PATH=/root/.ssh/id_ed25519

# Required only for password auth.
# SSH_PASSWORD=your_password_here
